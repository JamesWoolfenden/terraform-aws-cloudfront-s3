---
name: Pull Request
on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  # holden:ignore:HLD_CI_017: tracking @main deliberately to pick up CI improvements from this org's own shared workflow repo
  pr:
    uses: JamesWoolfenden/.github/.github/workflows/terraform-pr.yml@main
